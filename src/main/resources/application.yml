spring:
  application:
    name: springAdminServer
  boot:
    admin:
      client:
        url: http://localhost:8000
  datasource:
    name: mysql
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      driver-class-name: com.mysql.jdbc.Driver
      url: jdbc:mysql://rm-uf61dg1417y0zra2r225.mysql.rds.aliyuncs.com/fosun_merchant?useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true&useSSL=false&serverTimezone=Asia/Shanghai
      username: root
      password: Foliday#0101PF
      platform: mysql

server:
  port: 8001

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    enabled-by-default: true
    web:
      base-path: /actuator
      exposure:
        include: '*'

logging:
  level:
    com.example.springadminserver: debug
    com.chz.mapper: debug
